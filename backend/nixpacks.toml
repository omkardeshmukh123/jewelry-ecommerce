# Railway Nixpacks Configuration
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install --production=false"]

[phases.build]
cmds = ["echo 'No build step required'"]

[start]
cmd = "npm start"
